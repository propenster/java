import numpy as np
num_list = list(np.random.permutation(np.arrange(0,10))[:5])

print(num_list)

####
num_list = list(np.random.choice(np.arrange(0,10), 5, replace=False))
for k, v in question.items():
	print("What is the Capital of ", k)
print(num_list)